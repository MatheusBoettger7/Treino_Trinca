{
  "name": "treino-trinca",
  "version": "0.1.0",
  "private": true,
  "description": "Treino Trinca - aplicativo de treinos para Android, iOS e Web",
  "scripts": {
    "prepare:web": "node tools/prepare-web.mjs",
    "cap:sync": "npm run prepare:web && cap sync",
    "cap:add:android": "npm run prepare:web && cap add android",
    "cap:add:ios": "npm run prepare:web && cap add ios"
  },
  "dependencies": {
    "@capacitor/android": "8.0.0",
    "@capacitor/core": "8.0.0",
    "@capacitor/ios": "8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "8.0.0"
  }
}
